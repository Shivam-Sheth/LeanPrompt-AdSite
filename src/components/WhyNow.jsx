export default function WhyNow() {
  return (
    <section id="why-now" className="py-20 lg:py-28 bg-white">
      <div className="container mx-auto px-6 lg:px-12">
        <div className="max-w-3xl mx-auto text-center">
          <h2 className="font-display font-bold text-3xl sm:text-4xl text-neutral-900">
            Why now
          </h2>
          <p className="mt-6 text-lg text-neutral-600 leading-relaxed">
            AI usage is exploding. Individuals and enterprises are sending billions of prompts every month. Even small inefficiencies—a few extra tokens per query—add up to massive waste in compute, cost, and carbon. LeanPrompt introduces efficiency <em>before</em> the prompt ever reaches the model, so you get the same results with less.
          </p>
        </div>
      </div>
    </section>
  );
}
